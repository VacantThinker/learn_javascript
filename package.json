{
  "scripts": {
    "git-init": "git init && git remote add origin https://github.com/VacantThinker/learn_javascript.git",
    "push-master-branch": "git add . && git commit -m 'new' && git push origin master --force"
  }
}